{
  "name": "lodestone-sampler",
  "version": "1.1.0",
  "private": true,
  "description": "Block orientation sampler UI. TypeScript sources under src/, built to a single self-contained dist/index.html.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node build.mjs",
    "build:fast": "node build.mjs",
    "dev": "node build.mjs --watch --serve"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
